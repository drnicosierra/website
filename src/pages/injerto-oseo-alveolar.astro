---
import { getEntry } from 'astro:content';
import ServicePageTemplate from '../components/ServicePageTemplate.astro';

const entry = await getEntry('services', 'injerto-oseo-alveolar');
if (!entry) throw new Error('Missing content entry: injerto-oseo-alveolar');
---
<ServicePageTemplate entry={entry} />
