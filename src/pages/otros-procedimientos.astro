---
import { getEntry } from 'astro:content';
import ServicePageTemplate from '../components/ServicePageTemplate.astro';

const entry = await getEntry('services', 'otros-procedimientos');
if (!entry) throw new Error('Missing content entry: otros-procedimientos');
---
<ServicePageTemplate entry={entry} />
