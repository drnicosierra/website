---
import { getEntry } from 'astro:content';
import ServicePageTemplate from '../components/ServicePageTemplate.astro';

const entry = await getEntry('services', 'cirugia-revision-fisura');
if (!entry) throw new Error('Missing content entry: cirugia-revision-fisura');
---
<ServicePageTemplate entry={entry} />
